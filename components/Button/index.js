import React from 'react';
import styles from './Button.module.scss';

const PrimaryButton = () => (
  <button onClick={() => {}} className={styles['btn-primary']}>
    PrimaryButton
  </button>
);

export { PrimaryButton };
